<?php
// Mapeo de datos maestros y preventivos
$nombre = $p['nombre_completo'] ?? 'Sin Nombre';
$idPostulante = $p['id_postulante'] ?? 0;
$activo = $p['activo'] ?? 0;
$etapaId = $p['etapa_id'] ?? 1;
$generoId = $p['genero_id'] ?? null;
$viviendaId = $p['situacion_vivienda_id'] ?? null;
$puestoId = $p['puesto_id'] ?? null;
$catalogos = $catalogos ?? [];
?>

<div class="postulantes-container">
    <form id="formUpdateCompleto">
        <input type="hidden" name="id_postulante" value="<?= $idPostulante ?>">
        <div class="section-header" style="margin-bottom: 25px;">
            <div class="header-info">
                <p class="section-kicker">Expediente del Colaborador</p>
                <h2><?= htmlspecialchars($nombre) ?></h2>
            </div>
            <div class="header-actions">
                <button type="button" class="btn-refresh" onclick="window.location.href='?page=postulantes'" style="background:#6c757d;">Cancelar</button>
                <button type="submit" class="btn-edit" style="background:#28a745; color:white; padding: 12px 25px; font-weight:bold;">💾 ACTUALIZAR EXPEDIENTE COMPLETO</button>
            </div>
        </div>

        <div class="contenedor-grid-detalle" style="display: grid; grid-template-columns: 1fr 320px; gap: 20px;">

            <div class="col-main">

                <!-- 1. INFORMACIÓN DE PERFIL Y DATOS PERSONALES -->
                <div class="card-blanca" style="padding: 20px; border: 1px solid #ddd; border-radius: 8px; background: #fff; margin-bottom:20px;">
                    <h3 style="border-bottom: 2px solid #2c3e50; padding-bottom: 10px; margin-bottom: 20px;">Perfil y Datos Personales</h3>
                    <div class="form-grid" style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 15px;">
                        <div class="input-group">
                            <label>Nombres</label>
                            <input type="text" name="nombres" class="search-box input" value="<?= $p['nombres'] ?? '' ?>">
                        </div>
                        <div class="input-group">
                            <label>Apellidos</label>
                            <input type="text" name="apellidos" class="search-box input" value="<?= $p['apellidos'] ?? '' ?>">
                        </div>
                        <div class="input-group">
                            <label>DNI</label>
                            <input type="text" class="search-box input" style="background:#f0f0f0;" value="<?= $p['num_documento'] ?? '' ?>" readonly>
                        </div>
                        <div class="input-group">
                            <label>Género</label>
                            <select name="genero_id" class="search-box input">
                                <option value="">-- Seleccionar --</option>
                                <?php foreach ($catalogos['generos'] as $gen): ?>
                                    <option value="<?= $gen['id_genero'] ?>" <?= $generoId == $gen['id_genero'] ? 'selected' : '' ?>>
                                        <?= htmlspecialchars($gen['descripcion']) ?>
                                    </option>
                                <?php endforeach; ?>
                            </select>
                        </div>
                        <div class="input-group">
                            <label>Fecha de Nacimiento</label>
                            <input type="date" name="fecha_nacimiento" class="search-box input" value="<?= $p['fecha_nacimiento'] ?? '' ?>">
                        </div>
                        <div class="input-group">
                            <label>Situación de Vivienda</label>
                            <select name="situacion_vivienda_id" class="search-box input">
                                <option value="">-- Seleccionar --</option>
                                <?php foreach ($catalogos['viviendas'] as $viv): ?>
                                    <option value="<?= $viv['id_situacion'] ?>" <?= $viviendaId == $viv['id_situacion'] ? 'selected' : '' ?>>
                                        <?= htmlspecialchars($viv['descripcion']) ?>
                                    </option>
                                <?php endforeach; ?>
                            </select>
                        </div>
                        <div class="input-group">
                            <label>Correo Electrónico</label>
                            <input type="email" name="email" class="search-box input" value="<?= $p['email'] ?? '' ?>">
                        </div>
                        <div class="input-group">
                            <label>Teléfono</label>
                            <input type="text" name="telefono" class="search-box input" value="<?= $p['telefono'] ?? '' ?>">
                        </div>
                        <div class="input-group">
                            <label>Distrito</label>
                            <input type="text" name="distrito" class="search-box input" value="<?= $p['distrito'] ?? '' ?>">
                        </div>
                        <div class="input-group" style="grid-column: span 3;">
                            <label>Dirección Exacta</label>
                            <input type="text" name="direccion" class="search-box input" style="width: 100%;" value="<?= $p['direccion'] ?? '' ?>">
                        </div>
                    </div>
                </div>

                <!-- NUEVA SECCIÓN: PUESTOS ASIGNADOS (MULTIFUNCIONAL) -->
                <div class="card-blanca" style="padding: 20px; border: 1px solid #ddd; border-radius: 8px; background: #fff; margin-bottom:20px;">
                    <div style="display: flex; justify-content: space-between; align-items: center; border-bottom: 2px solid #f39c12; padding-bottom: 10px; margin-bottom: 20px;">
                        <h3 style="margin:0;">Puestos Asignados (Multifuncional)</h3>
                        <button type="button" onclick="agregarFilaPuesto()" class="btn-refresh" style="font-size: 0.8rem; padding: 5px 10px; background: #f39c12;">+ Asignar otro Puesto</button>
                    </div>
                    <div id="contenedor-puestos" style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px;">
                        <?php
                        // Obtenemos los puestos actuales desde el array de la consulta completa
                        // Nota: Asegúrate que AdminRepository.php devuelva un array 'postulaciones'
                        if (!empty($p['postulaciones'])):
                            foreach ($p['postulaciones'] as $idx => $pos): ?>
                                <div class="fila-dinamica" style="display: flex; gap: 10px; padding: 10px; background: #fef5e7; border-radius: 5px; align-items: center;">
                                    <select name="puestos[<?= $idx ?>][id_puesto]" class="search-box input" style="flex:1;">
                                        <?php foreach ($catalogos['puestos'] as $cat): ?>
                                            <option value="<?= $cat['id_puesto'] ?>" <?= ($pos['puesto_id'] == $cat['id_puesto']) ? 'selected' : '' ?>>
                                                <?= htmlspecialchars($cat['descripcion']) ?>
                                            </option>
                                        <?php endforeach; ?>
                                    </select>
                                    <button type="button" onclick="this.parentElement.remove()" style="color:#e74c3c; border:none; background:none; cursor:pointer;">✕</button>
                                </div>
                        <?php endforeach;
                        endif; ?>
                    </div>
                </div>

                <!-- 2. HABILIDADES Y COMPETENCIAS (DINÁMICA) -->
                <div class="card-blanca" style="padding: 20px; border: 1px solid #ddd; border-radius: 8px; background: #fff; margin-bottom:20px;">
                    <div style="display: flex; justify-content: space-between; align-items: center; border-bottom: 2px solid #27ae60; padding-bottom: 10px; margin-bottom: 20px;">
                        <h3 style="margin:0;">Habilidades (Skills)</h3>
                        <button type="button" onclick="agregarFilaSkill()" class="btn-refresh" style="font-size: 0.8rem; padding: 5px 10px;">+ Vincular Habilidad</button>
                    </div>
                    <!-- SECCIÓN DE SKILLS CORREGIDA (Sin Warnings) -->
                    <div id="contenedor-skills" style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px;">
                        <?php if (!empty($p['skills'])): foreach ($p['skills'] as $index => $sk): ?>
                                <div class="fila-dinamica" style="display: flex; gap: 10px; padding: 10px; background: #f1f8f4; border-radius: 5px; align-items: center;">
                                    <select name="skills[<?= $index ?>][skill_id]" class="search-box input" style="flex:2;">
                                        <option value="">-- Seleccionar Habilidad --</option>
                                        <?php foreach ($catalogos['skills'] as $cat): ?>
                                            <option value="<?= $cat['id_skill'] ?>" <?= (isset($sk['skill_id']) && $sk['skill_id'] == $cat['id_skill']) ? 'selected' : '' ?>>
                                                <?= htmlspecialchars($cat['descripcion']) ?>
                                            </option>
                                        <?php endforeach; ?>
                                    </select>
                                    <select name="skills[<?= $index ?>][nivel_id]" class="search-box input" style="flex:1;">
                                        <option value="1" <?= (isset($sk['nivel_id']) && $sk['nivel_id'] == 1) ? 'selected' : '' ?>>Básico</option>
                                        <option value="2" <?= (isset($sk['nivel_id']) && $sk['nivel_id'] == 2) ? 'selected' : '' ?>>Intermedio</option>
                                        <option value="3" <?= (isset($sk['nivel_id']) && $sk['nivel_id'] == 3) ? 'selected' : '' ?>>Avanzado</option>
                                    </select>
                                    <button type="button" onclick="this.parentElement.remove()" style="color:#e74c3c; border:none; background:none; cursor:pointer;">✕</button>
                                </div>
                        <?php endforeach;
                        endif; ?>
                    </div>
                </div>

                <!-- 2. EXPERIENCIA LABORAL (DINÁMICA) -->
                <div class="card-blanca" style="padding: 20px; border: 1px solid #ddd; border-radius: 8px; background: #fff; margin-bottom:20px;">
                    <div style="display: flex; justify-content: space-between; align-items: center; border-bottom: 2px solid #3498db; padding-bottom: 10px; margin-bottom: 20px;">
                        <h3 style="margin:0;">Experiencia Laboral</h3>
                        <button type="button" onclick="agregarFilaExperiencia()" class="btn-refresh" style="font-size: 0.8rem; padding: 5px 10px;">+ Agregar</button>
                    </div>
                    <div id="contenedor-experiencias">
                        <?php if (!empty($p['experiencias'])): foreach ($p['experiencias'] as $index => $exp): ?>
                                <div class="fila-dinamica" style="display: grid; grid-template-columns: 1fr 1fr 1fr auto; gap: 10px; margin-bottom: 10px; padding: 10px; background: #f9f9f9; border-radius: 5px;">
                                    <input type="text" name="experiencias[<?= $index ?>][empresa]" placeholder="Empresa" value="<?= $exp['empresa'] ?>" class="search-box input">
                                    <input type="text" name="experiencias[<?= $index ?>][cargo]" placeholder="Cargo" value="<?= $exp['cargo'] ?>" class="search-box input">
                                    <div style="display: flex; gap: 5px;">
                                        <input type="date" name="experiencias[<?= $index ?>][fecha_inicio]" value="<?= $exp['fecha_inicio'] ?>" class="search-box input" style="font-size: 0.7rem;">
                                        <input type="date" name="experiencias[<?= $index ?>][fecha_fin]" value="<?= $exp['fecha_fin'] ?>" class="search-box input" style="font-size: 0.7rem;">
                                    </div>
                                    <button type="button" onclick="this.parentElement.remove()" style="background:none; border:none; color:#e74c3c; cursor:pointer;">✕</button>
                                </div>
                        <?php endforeach;
                        endif; ?>
                    </div>
                </div>

                <!-- 3. ESTUDIOS (DINÁMICA) -->
                <div class="card-blanca" style="padding: 20px; border: 1px solid #ddd; border-radius: 8px; background: #fff;">
                    <div style="display: flex; justify-content: space-between; align-items: center; border-bottom: 2px solid #9b59b6; padding-bottom: 10px; margin-bottom: 20px;">
                        <h3 style="margin:0;">Formación Académica</h3>
                        <button type="button" onclick="agregarFilaEstudio()" class="btn-refresh" style="font-size: 0.8rem; padding: 5px 10px;">+ Agregar</button>
                    </div>
                    <!-- SECCIÓN DE ESTUDIOS CORREGIDA -->
                    <div id="contenedor-estudios">
                        <?php if (!empty($p['estudios'])): foreach ($p['estudios'] as $index => $est): ?>
                                <div class="fila-dinamica" style="display: grid; grid-template-columns: 1.5fr 1fr 1fr 1fr auto; gap: 10px; margin-bottom: 10px; padding: 15px; background: #f9f9f9; border-radius: 5px;">

                                    <div class="input-group">
                                        <label>Institución</label>
                                        <select name="estudios[<?= $index ?>][institucion_id]" class="search-box input">
                                            <option value="">-- Seleccionar --</option>
                                            <?php foreach ($catalogos['instituciones'] as $inst): ?>
                                                <option value="<?= $inst['id_institucion'] ?>" <?= (isset($est['institucion_id']) && $est['institucion_id'] == $inst['id_institucion']) ? 'selected' : '' ?>>
                                                    <?= htmlspecialchars($inst['descripcion']) ?>
                                                </option>
                                            <?php endforeach; ?>
                                        </select>
                                    </div>

                                    <div class="input-group">
                                        <label>Grado / Tipo</label>
                                        <select name="estudios[<?= $index ?>][tipo_id]" class="search-box input">
                                            <?php foreach ($catalogos['tipos_est'] as $tipo): ?>
                                                <option value="<?= $tipo['id_tipo'] ?>" <?= $est['tipo_id'] == $tipo['id_tipo'] ? 'selected' : '' ?>>
                                                    <?= htmlspecialchars($tipo['descripcion']) ?>
                                                </option>
                                            <?php endforeach; ?>
                                        </select>
                                    </div>

                                    <div class="input-group">
                                        <label>Estado</label>
                                        <select name="estudios[<?= $index ?>][estado_id]" class="search-box input">
                                            <?php foreach ($catalogos['estados_est'] as $estado): ?>
                                                <option value="<?= $estado['id_estado'] ?>" <?= $est['estado_id'] == $estado['id_estado'] ? 'selected' : '' ?>>
                                                    <?= htmlspecialchars($estado['descripcion']) ?>
                                                </option>
                                            <?php endforeach; ?>
                                        </select>
                                    </div>

                                    <div class="input-group">
                                        <label>Periodo (Inicio - Fin)</label>
                                        <div style="display: flex; gap: 5px;">
                                            <input type="date" name="estudios[<?= $index ?>][fecha_inicio]" value="<?= $est['fecha_inicio'] ?>" class="search-box input" style="font-size: 0.75rem;">
                                            <input type="date" name="estudios[<?= $index ?>][fecha_fin]" value="<?= $est['fecha_fin'] ?>" class="search-box input" style="font-size: 0.75rem;">
                                        </div>
                                    </div>

                                    <button type="button" onclick="this.parentElement.remove()" style="background:none; border:none; color:#e74c3c; cursor:pointer; align-self: center;">✕</button>
                                </div>
                        <?php endforeach;
                        endif; ?>
                    </div>
                </div>
            </div>

            <!-- COLUMNA DERECHA: PANEL DE CONTROL -->
            <div class="col-sidebar">
                <div class="card-control" style="background: #f8f9fa; padding: 20px; border: 1px solid #ccc; border-radius: 8px; position: sticky; top: 20px;">
                    <h3 style="font-size: 0.9rem; margin-bottom: 15px;">Logística de Botica</h3>

                    <div class="control-item" style="margin-bottom: 20px;">
                        <label style="font-weight: bold; font-size: 0.7rem;">TURNO PREFERIDO</label>
                        <div style="margin-top:10px;">
                            <label style="display:block; font-size:0.8rem; margin-bottom:5px;">
                                <input type="checkbox" name="turnos[]" value="1" <?= in_array(1, array_column($p['turnos'] ?? [], 'turno_id')) ? 'checked' : '' ?>> Mañana
                            </label>
                            <label style="display:block; font-size:0.8rem;">
                                <input type="checkbox" name="turnos[]" value="2" <?= in_array(2, array_column($p['turnos'] ?? [], 'turno_id')) ? 'checked' : '' ?>> Tarde
                            </label>
                        </div>
                    </div>

                    <hr>

                    <div class="control-item" style="margin: 20px 0;">
                        <label style="font-weight: bold; font-size: 0.7rem;">ETAPA DE PROCESO</label>
                        <select name="etapa_id" class="search-box input" style="width: 100%;">
                            <?php foreach ($catalogos['etapas'] as $et): ?>
                                <option value="<?= $et['id_etapa'] ?>" <?= $etapaId == $et['id_etapa'] ? 'selected' : '' ?>>
                                    <?= htmlspecialchars($et['descripcion']) ?>
                                </option>
                            <?php endforeach; ?>
                        </select>
                    </div>

                    <div class="control-item">
                        <label style="font-weight: bold; font-size: 0.7rem;">ESTADO DE CUENTA</label>
                        <select name="activo" class="search-box input" style="width: 100%;">
                            <option value="1" <?= $activo == 1 ? 'selected' : '' ?>>Habilitado</option>
                            <option value="0" <?= $activo == 0 ? 'selected' : '' ?>>Suspendido</option>
                        </select>
                    </div>
                </div>
            </div>
        </div>
    </form>
</div>

<script>
    // 1. Inicialización ÚNICA de contadores (Carga los valores actuales de PHP)
    let puestoCount = <?= count($p['postulaciones'] ?? []) ?>;
    let estCount = <?= count($p['estudios'] ?? []) ?>;
    let skCount = <?= count($p['skills'] ?? []) ?>;
    let expCount = <?= count($p['experiencias'] ?? []) ?>; // Contador para experiencia

    // 2. Función para Puestos (Multifuncional)[cite: 12, 17]
    function agregarFilaPuesto() {
        const html = `
        <div class="fila-dinamica" style="display: flex; gap: 10px; padding: 10px; background: #fef5e7; border-radius: 5px; align-items: center;">
            <select name="puestos[${puestoCount}][id_puesto]" class="search-box input" style="flex:1;">
                <option value="">-- Seleccionar Puesto --</option>
                <?php foreach ($catalogos['puestos'] as $cat): ?>
                    <option value="<?= $cat['id_puesto'] ?>"><?= htmlspecialchars($cat['descripcion']) ?></option>
                <?php endforeach; ?>
            </select>
            <button type="button" onclick="this.parentElement.remove()" style="color:#e74c3c; border:none; background:none; cursor:pointer;">✕</button>
        </div>`;
        document.getElementById('contenedor-puestos').insertAdjacentHTML('beforeend', html);
        puestoCount++;
    }

    // 3. Función para Habilidades[cite: 17]
    function agregarFilaSkill() {
        const html = `
        <div class="fila-dinamica" style="display: flex; gap: 10px; padding: 10px; background: #f1f8f4; border-radius: 5px; align-items: center;">
            <select name="skills[${skCount}][skill_id]" class="search-box input" style="flex:2;">
                <option value="">-- Seleccionar --</option>
                <?php foreach ($catalogos['skills'] as $cat): ?>
                    <option value="<?= $cat['id_skill'] ?>"><?= htmlspecialchars($cat['descripcion']) ?></option>
                <?php endforeach; ?>
            </select>
            <select name="skills[${skCount}][nivel_id]" class="search-box input" style="flex:1;">
                <option value="1">Básico</option>
                <option value="2">Intermedio</option>
                <option value="3">Avanzado</option>
            </select>
            <button type="button" onclick="this.parentElement.remove()" style="color:#e74c3c; border:none; background:none; cursor:pointer;">✕</button>
        </div>`;
        document.getElementById('contenedor-skills').insertAdjacentHTML('beforeend', html);
        skCount++;
    }

    // 4. Función para Estudios[cite: 17]
    function agregarFilaEstudio() {
        const html = `
        <div class="fila-dinamica" style="display: grid; grid-template-columns: 1.5fr 1fr 1fr 1fr auto; gap: 10px; margin-bottom: 10px; padding: 15px; background: #f9f9f9; border-radius: 5px;">
            <select name="estudios[${estCount}][institucion_id]" class="search-box input">
                <option value="">-- Institución --</option>
                <?php foreach ($catalogos['instituciones'] as $inst): ?>
                    <option value="<?= $inst['id_institucion'] ?>"><?= htmlspecialchars($inst['descripcion']) ?></option>
                <?php endforeach; ?>
            </select>
            <select name="estudios[${estCount}][tipo_id]" class="search-box input">
                <?php foreach ($catalogos['tipos_est'] as $tipo): ?>
                    <option value="<?= $tipo['id_tipo'] ?>"><?= htmlspecialchars($tipo['descripcion']) ?></option>
                <?php endforeach; ?>
            </select>
            <select name="estudios[${estCount}][estado_id]" class="search-box input">
                <?php foreach ($catalogos['estados_est'] as $estado): ?>
                    <option value="<?= $estado['id_estado'] ?>"><?= htmlspecialchars($estado['descripcion']) ?></option>
                <?php endforeach; ?>
            </select>
            <div style="display: flex; gap: 5px;">
                <input type="date" name="estudios[${estCount}][fecha_inicio]" class="search-box input" style="font-size: 0.75rem;">
                <input type="date" name="estudios[${estCount}][fecha_fin]" class="search-box input" style="font-size: 0.75rem;">
            </div>
            <button type="button" onclick="this.parentElement.remove()" style="background:none; border:none; color:#e74c3c; cursor:pointer; align-self: center;">✕</button>
        </div>`;
        document.getElementById('contenedor-estudios').insertAdjacentHTML('beforeend', html);
        estCount++;
    }

    // 5. Función para Experiencia (Indispensable para que no falle el botón)[cite: 17]
    function agregarFilaExperiencia() {
        const html = `
        <div class="fila-dinamica" style="display: grid; grid-template-columns: 1fr 1fr 1fr auto; gap: 10px; margin-bottom: 10px; padding: 10px; background: #f9f9f9; border-radius: 5px;">
            <input type="text" name="experiencias[${expCount}][empresa]" placeholder="Empresa" class="search-box input">
            <input type="text" name="experiencias[${expCount}][cargo]" placeholder="Cargo" class="search-box input">
            <div style="display: flex; gap: 5px;">
                <input type="date" name="experiencias[${expCount}][fecha_inicio]" class="search-box input" style="font-size: 0.7rem;">
                <input type="date" name="experiencias[${expCount}][fecha_fin]" class="search-box input" style="font-size: 0.7rem;">
            </div>
            <button type="button" onclick="this.parentElement.remove()" style="background:none; border:none; color:#e74c3c; cursor:pointer;">✕</button>
        </div>`;
        document.getElementById('contenedor-experiencias').insertAdjacentHTML('beforeend', html);
        expCount++;
    }
</script>